version: 2

linters:
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused

formatters:
  enable:
    - gofmt
    - goimports

formatters-settings:
  gofmt:
    simplify: true
  goimports:
    local-prefixes: github.com/hyphaene/hexa

run:
  timeout: 5m
  skip-dirs:
    - vendor
    - .git

issues:
  exclude-use-default: false
  max-issues-per-linter: 0
  max-same-issues: 0
